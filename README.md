# Quark-2.0
Quark 2.0 is a programming language specifically designed to get used to real scripting rather than playing with code blocks, its much easier than python. Any quark file(.qk) will run with its parent program in python serving as its interpreter.
